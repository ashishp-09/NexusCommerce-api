{
  "openapi": "3.0.3",
  "info": {
    "title": "NexusCommerce REST API",
    "description": "High-performance, modular enterprise e-commerce backend built with TypeScript, Express, Prisma ORM, Redis caching, and Stripe payments.",
    "version": "1.1.0",
    "contact": {
      "name": "NexusCommerce Engineering Team",
      "url": "https://nexuscommerce.io"
    },
    "license": {
      "name": "MIT",
      "url": "https://opensource.org/licenses/MIT"
    }
  },
  "servers": [
    {
      "url": "http://localhost:3000/api/v1",
      "description": "Local Development Server"
    },
    {
      "url": "https://api.nexuscommerce.io/api/v1",
      "description": "Production Server"
    }
  ],
  "paths": {
    "/health": {
      "get": {
        "summary": "Service Health Check",
        "description": "Returns uptime, operational status, and server metadata",
        "responses": {
          "200": { "description": "Server is healthy and operational" }
        }
      }
    },
    "/health/detailed": {
      "get": {
        "summary": "Detailed System Diagnostics",
        "description": "Returns database latency, Redis health, and memory metrics",
        "responses": {
          "200": { "description": "Diagnostic telemetry metrics" }
        }
      }
    },
    "/auth/register": {
      "post": {
        "summary": "Register New User",
        "description": "Creates a new customer account and sends email verification code",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["username", "email", "password", "firstName", "lastName"],
                "properties": {
                  "username": { "type": "string", "example": "johndoe" },
                  "email": { "type": "string", "format": "email", "example": "john@example.com" },
                  "password": { "type": "string", "example": "SuperSecret123!" },
                  "firstName": { "type": "string", "example": "John" },
                  "lastName": { "type": "string", "example": "Doe" }
                }
              }
            }
          }
        },
        "responses": {
          "201": { "description": "User created successfully" },
          "400": { "description": "Validation error or user already exists" }
        }
      }
    },
    "/auth/login": {
      "post": {
        "summary": "User Login",
        "description": "Authenticates user credentials and issues JWT cookies/tokens",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["email", "password"],
                "properties": {
                  "email": { "type": "string", "example": "john@example.com" },
                  "password": { "type": "string", "example": "SuperSecret123!" }
                }
              }
            }
          }
        },
        "responses": {
          "200": { "description": "Authentication successful" },
          "401": { "description": "Invalid credentials" }
        }
      }
    },
    "/product": {
      "get": {
        "summary": "Get Paginated Product Catalog",
        "description": "Fetches products with multi-facet filtering, sorting, and Redis caching",
        "parameters": [
          { "name": "page", "in": "query", "schema": { "type": "integer", "default": 1 } },
          { "name": "limit", "in": "query", "schema": { "type": "integer", "default": 20 } }
        ],
        "responses": {
          "200": { "description": "Products retrieved successfully" }
        }
      }
    },
    "/product/search/suggestions": {
      "get": {
        "summary": "Search Autocomplete Suggestions",
        "parameters": [
          { "name": "q", "in": "query", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Typeahead suggestions list" }
        }
      }
    },
    "/product/facets": {
      "get": {
        "summary": "Catalog Facet Aggregations",
        "responses": {
          "200": { "description": "Category item counts and price boundaries" }
        }
      }
    },
    "/product/featured": {
      "get": {
        "summary": "Get Featured Products",
        "responses": {
          "200": { "description": "Featured products list" }
        }
      }
    },
    "/cart": {
      "get": {
        "summary": "Get User Cart",
        "responses": {
          "200": { "description": "Current cart with item totals and subtotal" }
        }
      },
      "post": {
        "summary": "Add Item to Cart",
        "responses": {
          "200": { "description": "Item added to cart" }
        }
      }
    },
    "/wishlist": {
      "get": {
        "summary": "Get Customer Wishlist",
        "responses": {
          "200": { "description": "User saved wishlist items" }
        }
      },
      "post": {
        "summary": "Add Product to Wishlist",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["productId"],
                "properties": {
                  "productId": { "type": "string" }
                }
              }
            }
          }
        },
        "responses": {
          "201": { "description": "Product added to wishlist" }
        }
      }
    },
    "/wishlist/{productId}/move-to-cart": {
      "post": {
        "summary": "Move Wishlist Item to Shopping Cart",
        "parameters": [
          { "name": "productId", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Item moved to cart" }
        }
      }
    },
    "/coupons": {
      "get": {
        "summary": "List Available Coupons & Promotions",
        "responses": {
          "200": { "description": "Active promotional coupons" }
        }
      }
    },
    "/coupons/apply": {
      "post": {
        "summary": "Apply Promo Code to Cart",
        "responses": {
          "200": { "description": "Coupon applied and discount calculated" }
        }
      }
    },
    "/orders": {
      "post": {
        "summary": "Create Order from Cart",
        "responses": {
          "201": { "description": "Order created" }
        }
      }
    },
    "/payment/checkout": {
      "post": {
        "summary": "Create Stripe Checkout Session",
        "responses": {
          "200": { "description": "Stripe checkout URL and session ID" }
        }
      }
    }
  }
}
